type Props = {
    cityName: string;
    latitude: number;
    longtitude: number;
};

export default function CityCard(props: Props) {
    return (
        <div className="">
            <img></img>
        </div>
    );
}
